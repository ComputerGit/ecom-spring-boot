# application-prod.yml

spring:
  application:
    name: eCommerce
  datasource:
    url: jdbc:postgresql://localhost:5432/eCommerce
    username: postgres
    password: Rahul@123
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER
  data:
    redis:
      host: valkeycacheyt.go2c0b.ng.0001.use1.cache.amazonaws.com
      port: 6378  # use spring.data.redis.port instead of the deprecated spring.redis.port

jwt:
  secret: JustinBieberisBabyandSongsThatareBieBermania

discount:
  types: PERCENTAGE,FLAT_RATE,BUY_ONE_GET_ONE,FREE_SHIPPING

change:
  types: ADD,REMOVE,ADJUST

read:
  status: READ,UNREAD

notification:
  types: ORDER_UPDATE,PROMOTION,REMINDER,SYSTEM_ALERT

SECRET_KEY: jUSTINbIEBERisChrisBrown

aws:
  region: ap-southeast-2
  kms:
    key-id: mrk-b98d3b34b79440d2ba54f289f5a027e8
